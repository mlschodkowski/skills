# Simple Language

Use `$simple-language` to make prose clear, direct, and easy to act on with low cognitive load. It keeps facts, uncertainty, exact technical terms, and the writer's voice.

## Example

| BEFORE | AFTER |
| --- | --- |
| Because the service's horizontally distributed cache invalidation mechanism relies on a combination of versioned keys and asynchronous fan-out, operators should first confirm that the deployment has achieved a stable state before initiating a manual flush. It is important to note that the cache eviction endpoint, although generally safe, can potentially create a temporary increase in database load if invoked while the index rebuild is in progress. In order to ensure that stale catalog data is not propagated further, verify that the `catalog-indexer` deployment is available, that the `cache_invalidation_lag_seconds` metric is below 30, and that the last successful event has been acknowledged by every consumer. If the metric remains elevated, consideration should be given to restarting the `catalog-cache` workers, but this should not be performed without first preserving the relevant logs because the restart may obscure the original failure mode. The following command can be used to inspect the relevant state: `kubectl get deployment catalog-cache -n production`. | Before starting a manual cache flush, confirm that the deployment is stable. The service uses versioned keys and asynchronous fan-out to invalidate its distributed cache.<br><br>Check that:<br><br>• The `catalog-indexer` deployment is available.<br>• `cache_invalidation_lag_seconds` is below 30.<br>• Every consumer has acknowledged the last successful event.<br><br>These checks help prevent stale catalog data from spreading. The cache eviction endpoint is generally safe, but it can temporarily increase database load during an index rebuild.<br><br>If the metric remains elevated, consider restarting the `catalog-cache` workers. First preserve the relevant logs, because a restart may hide the original failure.<br><br>Inspect the cache deployment with:<br><br>`kubectl get deployment catalog-cache -n production` |
